[build-system]
requires = ["setuptools>=42", "wheel", "pybind11>=2.10", "numpy"]
build-backend = "setuptools.build_meta"

[project]
name = "tmm_faster"
version = "0.1.0"
description = "High-performance TMM calculation with OpenMP"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "numpy>=1.16",
]